# Next.js — copie para .env e/ou .env.local (ver ordem de carregamento na documentação Next).
# .env.local tem prioridade sobre .env para a mesma chave (ficheiro local não vai para o git).

NEXT_PUBLIC_API_URL=http://localhost:3002/api
NEXT_PUBLIC_WS_URL=http://localhost:3002

# URL pública do MinIO (mesmo host/porta que o backend usa em POST /api/public/upload).
# Usada no browser para mapear ficheiros para o proxy /storage/* do Next (pré-visualização nos cartões).
NEXT_PUBLIC_MINIO_PUBLIC_URL=http://localhost:9010

# Opcional: destino do rewrite /storage/* (default http://localhost:9010). Em produção, URL pública do object storage.
# MINIO_PUBLIC_BROWSER_URL=https://files.seudominio.com

# Opcional: usado em alguns scripts / proxies
# BACKEND_URL=http://localhost:3002

# Porta do dev server (o script `npm run dev` também fixa -p 3010)
PORT=3010

# MFA, Brevo, JWT, DB, etc. — ficam em backend/.env (Nest carrega só essa pasta ao correr a API).
