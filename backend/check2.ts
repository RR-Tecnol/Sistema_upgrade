import { PrismaClient } from '@prisma/client'; import * as bcrypt from 'bcryptjs'; const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ select: { email: true, password: true }}); for (const u of users) { console.log(u.email, bcrypt.compareSync('RR@@Upgrade', u.password)); } } main();
